import{create}from'zustand';import{invoke}from'@tauri-apps/api/core';
export type Model={id:string,name:string,task:string,quant:string,size:string,score:number};
type HW={cpu:string,ram:string,gpu:string,backend:string};type State={page:string,setPage:(p:string)=>void,hardware:HW,models:Model[],running:string,scan:()=>Promise<void>,run:(name:string)=>Promise<void>};
export const useStudio=create<State>((set)=>({page:'dashboard',setPage:page=>set({page}),hardware:{cpu:'در حال شناسایی…',ram:'در حال شناسایی…',gpu:'در حال شناسایی…',backend:'در حال شناسایی…'},models:[{id:'qwen3-8b',name:'Qwen3 8B',task:'گفتگو / استدلال',quant:'Q4_K_M',size:'≈ 5 GB',score:96},{id:'llama-3.1-8b',name:'Llama 3.1 8B',task:'گفتگو / کدنویسی',quant:'Q4_K_M',size:'≈ 5 GB',score:94},{id:'qwen2.5-coder-7b',name:'Qwen2.5 Coder 7B',task:'کدنویسی',quant:'Q4_K_M',size:'≈ 4.7 GB',score:92}],running:'',scan:async()=>{try{const h=await invoke<{os:string,cpu:string,cores:number,ram_gb:number,gpus:string[]}>('scan_hardware');set({hardware:{cpu:`${h.cpu} · ${h.cores} هسته`,ram:`${h.ram_gb} GB`,gpu:h.gpus.length?h.gpus.join('، '):'GPU شناسایی نشد',backend:h.gpus.length?'GPU + CPU':'CPU'}})}catch{set({hardware:{cpu:'وب/حالت توسعه',ram:'—',gpu:'—',backend:'شبیه‌ساز توسعه'}})},run:async(name)=>{try{await invoke('start_model',{name})}finally{set({running:name})}}}));
